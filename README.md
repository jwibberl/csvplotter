# CSV Plotter

This will plot values from the CSV files generated by a SpeedyBee F405 Wing Mini. Usually *x* should be "Time" and *y* should be one of the other fields that this program will print out. Only some of the fields will make sense in the generated image.

It will generate "output.png".
